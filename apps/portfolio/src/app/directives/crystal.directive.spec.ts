import { CrystalDirective } from './crystal.directive';

describe('CrystalDirective', () => {
	it('should create an instance', () => {
		const directive = new CrystalDirective();
		expect(directive).toBeTruthy();
	});
});
